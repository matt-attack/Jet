
extern fun int printf(char* h, int h);
extern fun char* malloc(int x);

extern fun int puts(char* str);

fun int main() 
{ 
	let x = 1;
	
	if (x == 0)
	{
		puts("fail");
	}
	else if (x == 1)
	{
		puts("pass");
	}
	else
	{
		puts("fail");
	}
	
	return 0;
}